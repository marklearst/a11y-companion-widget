/**
 * Effects barrel: re-export effect factories and defaults from a single surface.
 */
export { dropShadowEffect, createDropShadowEffect, DROP_SHADOW_DEFAULTS } from './dropShadows'

// Optional: re-export effect-related types for convenience
export type { DropShadowEffect } from 'types'
